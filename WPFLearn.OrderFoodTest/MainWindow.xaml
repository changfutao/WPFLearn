<Window x:Class="WPFLearn.OrderFoodTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFLearn.OrderFoodTest"
        mc:Ignorable="d"
        Title="{Binding Restaurant.Name, StringFormat=\{0\}-在线订餐}" Height="450" Width="800" FontSize="20">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Border BorderBrush="IndianRed" BorderThickness="3" CornerRadius="6">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock>名称: </TextBlock>
                    <TextBlock Text="{Binding Restaurant.Name}"></TextBlock>
                </StackPanel>
                <TextBlock Text="{Binding Restaurant.Address, StringFormat=地址: \{0\}}"></TextBlock>
                <TextBlock Text="{Binding Restaurant.Phone, StringFormat=电话: \{0\}}"></TextBlock>
            </StackPanel>
        </Border>
        <DataGrid Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" ItemsSource="{Binding DishMenu}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="名称" Width="120" Binding="{Binding Dish.Name}"></DataGridTextColumn>
                <DataGridTextColumn Header="类别" Width="120" Binding="{Binding Dish.Category}"></DataGridTextColumn>
                <DataGridTextColumn Header="点评" Width="120" Binding="{Binding Dish.Comment}"></DataGridTextColumn>
                <DataGridTextColumn Header="评分" Width="120" Binding="{Binding Dish.Score}"></DataGridTextColumn>
                <DataGridTemplateColumn Header="选择" SortMemberPath="IsSelected" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding Path=IsSelected, UpdateSourceTrigger=PropertyChanged}" Command="{Binding DataContext.SelectedCommand,RelativeSource={RelativeSource AncestorType={x:Type Window}}}"></CheckBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
            <TextBox Text="{Binding Count}" Width="120" Margin="10, 8"></TextBox>
            <Button  Margin="10, 8">订餐</Button>
        </StackPanel>
    </Grid>
</Window>
